---
"@memofs/core": minor
"@memofs/json-rpc": minor
"@memofs/mcp-server": minor
"@memofs/server": minor
"@memofs/connectors": minor
"@memofs/benchmark-kit": minor
"@memofs/testing": minor
"@memofs/adapter-ai-sdk": minor
"@memofs/adapter-openai": minor
"@memofs/adapter-r2": minor
"@memofs/adapter-transformers": minor
"@memofs/adapter-turso": minor
"@memofs/adapter-voyage": minor
"@memofs/adapter-workers-ai": minor
"@memofs/cli": minor
---

# MemoFS 1.3.0-beta.2 — Typed note IDs, write idempotency deduplication, and multi-dimensional status modeling

Seventh beta, published together at `1.3.0-beta.2` across all 15 packages with the `beta` npm dist-tag. This release introduces first-class typed note IDs in document schemas, create-time idempotency deduplication on memory writes, and orthogonal status dimensions (disputed, stale, unverified) for graph entities and facts.

## Core

### Added

- Added `id?: string` as a first-class typed property on `TimestampedNote` and `ConversationEntry` document interfaces.
- Added frontmatter and metadata normalization and serialization support for note IDs in `normalizeTimestampedNote` and `formatTimestampedNote`.
- Added `idempotencyKey?: string` to `WriteMemoryInput` with runtime deduplication in `writeMemory()`, returning `{ created: false }` without redundant appends to `notes.md` or `memory-events.jsonl` when duplicate keys or existing IDs are provided.
- Added orthogonal status dimensions (`disputed?: boolean`, `stale?: boolean`, `unverified?: boolean`) to `GraphNode`, `GraphEdge`, `GraphNodeInput`, and `GraphEdgeInput` to prevent semantic collisions across temporal deprecation, dispute, code drift, and time decay.
- Updated `markConflictingEdges()` to set `disputed: true` alongside `status: "conflicted"`.

## Stability

This is a **beta**. The public surface is intended to be stable through `1.0.0`; the `1.3.0-beta.x` line adds new capabilities and hardens existing ones while preserving the v1 read-by-default server contract and the beta npm dist-tag.

## Install

```bash
npm install @memofs/core
npm install @memofs/cli
npm install @memofs/mcp-server
npm install @memofs/adapter-openai
# ...etc
```

Documentation: https://docs.memofs.dev
