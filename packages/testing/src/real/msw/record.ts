/**
 * MSW fixture recorder — real implementation in ticket 64.
 *
 * For ticket 60, this is a stub that validates the script entry exists
 * (`pnpm msw:record`) and exits with a clear message.
 */

console.log("[memofs/testing] msw:record stub — real implementation in ticket 64");
console.log("Fixtures will be recorded from real APIs with redacted secrets.");
console.log("For now, no action taken.");
