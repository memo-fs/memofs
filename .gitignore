.agents/skills/
skills-lock.json
social
.opencode
.commandcode
apps/cloud
# Dependencies
node_modules
.pnp
.pnp.js
.pnpm-store

# Local env files
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Testing
coverage
benchmark-results
benchmarks/reports/*
!benchmarks/reports/.gitkeep

# Turbo
.turbo


build
dist


# Debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# Misc
.DS_Store
*.pem

# Editor / agent tool config (local-only, machine-specific)
# opencode.json
# skills-lock.json
.vscode/

# Agent runtime data
agent-sessions/

# Example agent runtime stores (demo memory created by examples/*)
examples/**/.memofs/

# Sub-package runtime data (regenerable; root .memofs/ is the project's own store)
packages/**/.memofs/
packages/**/.tekmemo/

# Connector secrets (ADR 0002): never synced, never committed. Tokens are resolved
# at run time from an opaque secretRef; this local file is a dev convenience only.
.memofs/secrets.json

# Wrangler build cache
.wrangler/

# Internal docs - not tracked
docs/adr/
docs/architecture/
docs/CONTEXT.md
# Internal tooling - not tracked
research/
skills-lock.json
.agents/skills/
