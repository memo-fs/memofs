/**
 * MSW setup placeholder for vitest.e2e.config.ts.
 *
 * Real implementation in ticket 64 (msw server + handlers).
 * For ticket 60, this is intentionally no-op so e2e config loads.
 */

/* no-op — will be replaced by setupServer in ticket 64 */
